# Simulacion -

This small repo contains implementations of several pseudo-random number
generation algorithms used in a Simulation course. Each algorithm is in its
own module. There's an interactive runner `run_algos.py` which asks for
parameters and prints results. Install `rich` for nicer console tables:

Then run:

python run_algos.py
